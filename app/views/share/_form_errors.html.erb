<% if form.errors.any? %>
  <div class="alert alert-danger" role="alert">
    <h5><%= pluralize(form.errors.count, "error") %> on form found.</h5>

    <ul>
      <% form.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>

